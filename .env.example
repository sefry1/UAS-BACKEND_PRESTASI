# Example Environment Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env to Git!

# Application
APP_PORT=3000

# JWT Configuration
# Generate with: openssl rand -base64 64
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_use_long_random_string

# PostgreSQL Connection
# Format: host=HOST user=USER password=PASSWORD dbname=DATABASE port=PORT sslmode=MODE
POSTGRES_DSN=host=localhost user=postgres password=YOUR_POSTGRES_PASSWORD dbname=prestasi_db port=5432 sslmode=disable

# MongoDB Connection
# Format without auth: mongodb://localhost:27017
# Format with auth: mongodb://username:password@localhost:27017/database?authSource=database
MONGO_URI=mongodb://YOUR_USERNAME:YOUR_PASSWORD@localhost:27017/prestasi_db_mongo?authSource=prestasi_db_mongo
MONGO_DB=prestasi_db_mongo

# Notes:
# 1. Generate strong passwords using: powershell -c "-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})"
# 2. For production, use environment-specific .env files (.env.production, .env.staging)
# 3. Consider using MongoDB Atlas for production (https://www.mongodb.com/cloud/atlas)
